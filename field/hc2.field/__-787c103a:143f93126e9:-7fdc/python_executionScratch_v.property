string>with g_TriMesh:
	square(0, 0, 500, 100, Vector4(0, 0, 0, 1), Vector4(1, 1, 1, 0.6))

with g_DiscMesh:
	disc(pos=(0, 0),
		 rgba=(1.0, 1.0, 1.0, 0.5),
		 radius=40.0,
		 fill=0.5)

f = FLine()
f += CFrame(r=0.1)

###

f = FLine().moveTo(0, 0, 10)
f.appendVectorText2("Hello", 0, 0, font="Optima", size=100)
f.bounds()[1]

_self.tintColour = Color4(0.3, 0.3, 0.3, 0.5)