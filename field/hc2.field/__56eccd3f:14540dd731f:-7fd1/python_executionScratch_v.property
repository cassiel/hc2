string>#line = FLine().moveTo(-100,0,100).lineTo(100,0,100)(color=Color4(1,0,0,0.5))

clear()
bigCentre("B")

clear()
bigCentre("ONE TWO")

if 1:
	d = 50
	ll = FLine(color=Vector4(1, 1, 1, 1))
	ll.moveTo(-d, -d).relLineTo(d, 0).relLineTo(0, d).lineTo(-d, -d)
	canvas.lines().clear()
	canvas.lines().add(ll)
